# FIXES.md
# RACOS verified solution registry. FX IDs are ecosystem-wide (RC004). Empty at onboarding.
---
| ID | FIXES | SOLUTION | SESSION |
|----|-------|----------|---------|
| FX001 | RC001 | Replaced SearchableSelect's `fixed inset-0` click-catcher div with a `useRef` root + document `mousedown` listener that closes the dropdown only on genuinely-outside clicks, without ever rendering an element that covers the rest of the page. | SES006 |
| FX002 | RC002 | Migration 0009 recreates business_profile / custom_rates / bookings (SQLite requires table recreation to change a column's REFERENCES target), repointing the three affected columns at `municipalities(id)`. Verified by replaying all 9 migrations with `PRAGMA foreign_keys=ON` (matching the real runtime) and reproducing the exact failing save — confirmed it now succeeds. | SES006 |
| FX003 | RC003 | Added submitError/saveError React state + try/catch/finally + a red dismissible error banner to each of the three silent save handlers, plus a submitting/saving indicator on the button. Any DB error now surfaces its exact message in the UI instead of failing invisibly. | SES007 |
| FX004 | RC004 | Deleted the local racos.db file so migrations reapply cleanly against a fresh file — a checksum mismatch on an already-migrated db is unrecoverable short of that. Standing rule going forward: treat a shipped migration file as immutable; append a new migration instead of editing one that may already be applied. | SES007 |
| FX005 | RC005 | Rewrote the breakdown-block loop around one shared `remaining` bucket per booking (base + additional payment combined). A block gets the full rate-based expected amount only if it's genuinely full-length and the bucket can still cover it; otherwise it takes whatever's left and every later block in that booking gets 0. Verified against all 9 rows across the user's 3 reference scenarios via a standalone node.js simulation before touching the real component. | SES008 |
| FX006 | RC006 | Added the two missing `Migration{}` entries (version 19, 20) to lib.rs's `local_db_migrations()`, pointing at the already-written 0019/0020 SQL files. Standing rule going forward: adding a migration file is only half the job here — it must also be registered by hand in src-tauri/src/lib.rs, and a full app restart (not just a frontend reload) is needed to apply it, since migrations run at Rust/Tauri startup. | SES009 |
| FX007 | RC007 | Reordered gps-ingest's device-id fallback chain to check `device.uniqueId` first. Diagnosed by temporarily writing raw payloads to a throwaway `gps_debug_log` table (Supabase's edge-function log tool only surfaces HTTP boundary lines, not the function's own console output) — dropped once confirmed. | SES010 |
| FX008 | RC008 | Added `key={auth.status}` to the `<LoginScreen>` element in App.tsx, forcing React to remount (not just re-render) on each auth-state transition so `initialMode` is re-read fresh. Also wrapped runBootstrap() in try/catch so any future unexpected failure falls back visibly to the sign-in screen instead of leaving the UI stuck silently. Diagnosed by querying `auth.users` directly via the Supabase MCP to confirm the sign-in itself had actually succeeded before looking for a frontend cause. | SES012 |
| FX009 | RC009 | Added `.catch()`/try-catch + a loadError state to HomeScreen and SettingsScreen's data-loading effects (BusinessName, BusinessHeadquarters), surfacing a red error message instead of hanging on "Loading…" forever. The underlying dev-only Fast Refresh desync itself needed no code fix — confirmed resolved by a full app restart, and not expected in a real production build. | SES013 |
| FX010 | RC010 | Not a real fix — worked around by hand-writing every portal source/config file directly (plain text writes, unaffected by the delete/rename restriction) and leaving `node_modules` for Falcon to generate locally via `npm install`, which is the normal Next.js workflow anyway (gitignored, never meant to be synced). Standing rule going forward: don't attempt `npm install`/build verification inside this sandbox for Node projects — write the code, defer install/build/test to Falcon's own machine. | SES014 |
| FX011 | RC011 | Ruled out a genuinely missing/corrupt native binary first (confirmed `lightningcss.win32-x64-msvc.node` present, correctly named, ~9.5MB, inside `node_modules/lightningcss-win32-x64-msvc/`) before concluding the failure was Turbopack's module resolution itself, not the install. Fix: `package.json`'s `dev`/`build` scripts changed to `next dev --webpack` / `next build --webpack`, forcing the classic webpack dev/build pipeline (standard Node `require()` resolution, no sandboxed evaluation step) instead of Turbopack. Verified end to end by Falcon — login screen and the authenticated dashboard (real owner name from the JWT) both render correctly under webpack. | SES014 |
