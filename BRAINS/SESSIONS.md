# SESSIONS.md
# RACOS completed session archive. Per-project (U011). Empty at onboarding.
---
## SES001 — 2026-07-25 — Phase 0 scaffold shipped
Progress made    : ROT001 — Tauri 2 + React + TS + Tailwind scaffold complete, identifier com.corelogix.racos confirmed against entERP precedent
Items moved      : ROT001 → DONE
Bugs found       : none
Fixes applied    : none
Lessons recorded : none
Carried forward  : ROT002 (Supabase project + RLS schema), ROT003 (SQLite cache + outbox table) — still NEXT
State at close   : RACOS onboarded via GAP protocol, Phase 0 scaffold shipped (Tauri 2.11.4, React 19.2.8, Tailwind 3.4.19, TS 5.8.3), RO/ROD/ROP/ROT registered. Mid-session, O006 was found wrong — TEMPORARIES.md was ecosystem-wide only in name; ET001 precedent showed project work belongs there. Amended O006 (Correction... no — Amendment, same ID, per user call) across BRAIN.md, ONBOARDING.md, ADOPTION.md, UPDATER.md, MANIFEST.md, REGISTRY.md. RACOS/_brain/TEMPORARIES.md created holding only ROT002/ROT003. User set a standing preference: no cross-project mentions in RACOS work going forward. Next session picks up at ROT002.

## SES002 — 2026-07-25 — Supabase project + RLS schema shipped
Progress made    : ROT002 — dedicated Supabase project confirmed (ref nnsjqnxvpkercbbwvqjj, region ap-northeast-1, FalconBit Org, free tier). Schema built: businesses (tenant root), profiles (extends auth.users), vehicles, customers, bookings, payments — all with business_id + RLS enabled. Helper fn current_business_id() (security definer, fixed search_path) resolves tenant scope from auth.uid() without recursive-policy issues on profiles. 5 migrations applied in order: init_multi_tenant_schema, rls_policies_business_id, harden_functions, optimize_rls_initplan, index_remaining_foreign_keys.
Items moved      : ROT002 → DONE. ROT003 unblocked (BLOCKED BY cleared).
Bugs found       : none
Fixes applied    : Security advisor flagged mutable search_path on trigger fn + public/anon execute on current_business_id() — fixed. Performance advisor flagged auth.uid() re-evaluated per row in businesses/profiles policies — rewritten as (select auth.uid()). Two FK columns missing covering indexes — indexed. Remaining WARN (current_business_id callable by authenticated via RPC) is inherent to the security-definer pattern RLS requires; accepted, not a data-exposure risk (only returns caller's own business_id).
Lessons recorded : User flagged a real security concern about connector access to secret keys before proceeding — resolved by confirming the Supabase MCP exposes no service-role-key tool at all (only get_publishable_keys), and by having them approve the create_project call individually. First create_project attempt showed "User rejected" but the project was created anyway (or already existed) — discovered via list_projects rather than assuming failure.
Carried forward  : ROT003 (SQLite local cache + outbox table) — now NEXT, unblocked.

## SES003 — 2026-07-25 — Local SQLite cache + outbox shipped
Progress made    : ROT003 — local SQLite schema mirroring the Supabase tables (businesses, profiles, vehicles, customers, bookings, payments), all SQLite-typed (TEXT for uuid/timestamp, TEXT for money to avoid float rounding). Added outbox table (queued insert/update/delete mutations, client-generated entity_id, status/retry/error tracking) and sync_state table (last_synced_at, offline_since — feeds ROD004's 5-day/50-record threshold check). bookings gained a local-only pending_availability_check flag operationalizing ROD003 (holds created offline await a live check on sync). Wired tauri-plugin-sql into Cargo.toml/package.json/lib.rs with migrations, granted sql capability permissions.
Items moved      : ROT003 → DONE.
Bugs found       : none
Fixes applied    : n/a
Lessons recorded : No cargo/rustc in the sandbox, so the Rust wiring couldn't be full `cargo check`'d — verified the SQL migrations directly instead (executed both files against a real SQLite db via Python's sqlite3, including FK and CHECK constraint behavior) and manually reviewed Cargo.toml/lib.rs/capabilities syntax.
Carried forward  : nothing queued in NEXT — TEMPORARIES.md/RACOS.md STATE both empty. User set a new standing preference mid-session: only update BRAIN files on explicit request or at session close, not after every change.

## SES004 — 2026-07-25 — Local-first desktop UI shipped (retroactive catch-up entry)
Progress made    : ROT004 — full screen set built against the local SQLite cache: DB access layer + TS entity types + CRUD repo functions with outbox writes; app shell with RACM-inspired sidebar; Vehicles/Customers/Bookings/Home screens; Checkout flow; two full dark-theme design-token restyles (Excel-grid tables, then Tabler-icon sidebar rebuild matched to a mockup, then sidebar resized to ~1/5 width with app-wide font bump); booking reference helper (RNT- prefix) and polished create form; app_settings table + settings repo/React context; date/time formatting utility + custom DatePicker/TimePicker components wired into the booking form; payment_amount/expected_payment schema + rental-duration settings + FormQuestion Google-Forms-style booking form; inline cancel confirmation; Rentals split into Ongoing/History subtabs.
Items moved      : ROT004 → DONE.
Bugs found       : none logged (BRAIN wasn't updated live across this span per the user's standing preference — this entry is a retroactive summary, not a session-by-session log).
Fixes applied    : n/a
Lessons recorded : none
Carried forward  : Rate Matrix / tier pricing work picked up next (ROT005).

## SES005 — 2026-07-25 — Rate Matrix + tier pricing shipped (retroactive catch-up entry)
Progress made    : ROT005 — migration for provinces/business_profile/seating_bands/rate_matrix; SearchableSelect filterable-dropdown component; Rate Matrix screen + sidebar nav item; Seats field added to Vehicles; destination + tier pricing wired into the booking form and Checkout. Follow-on migration added business-scoped `cities`, `custom_rates`, HQ city, destination city, and a booking Purpose field; pricing.ts resolveRate updated for custom-rate override priority (custom > standard matrix > vehicle daily_rate fallback); a Settings toggle controlled whether Tier-1 cities showed in the UI; Rate Matrix screen expanded for HQ city + city management + custom rates; booking form and Checkout updated for combined province/city destination + Purpose.
Items moved      : ROT005 → DONE.
Bugs found       : none logged (same retroactive-entry caveat as SES004).
Fixes applied    : n/a
Lessons recorded : none
Carried forward  : User flagged the business-scoped `cities` table + Tier-1 toggle as "absurd" and asked for real PSGC-sourced municipalities instead — picked up in SES006.

## SES006 — 2026-07-25 — PSGC municipalities, HQ relocation, and a save-breaking FK bug fixed
Progress made    : ROT006 — fetched full PSGC (Philippine Standard Geographic Code) city/municipality data region-by-region via web_fetch (avoided truncation seen on the full-country endpoint); built migration 0007 seeding a global `municipalities` table (1597 rows, joined to existing `provinces` by normalized name; Maguindanao's ~37 municipalities + Cotabato City intentionally excluded due to a province-split ambiguity between this PSGC snapshot and the already-seeded two-row Maguindanao del Norte/del Sur split — documented in the migration header). Migration 0008 dropped the business-scoped `cities` table and the `show_tier1_cities` app_settings column entirely: HQ's own province/city is now automatically Tier 1 with no separate toggle, and Custom Rates were redesigned to a structured province-then-municipality SearchableSelect (no free-text destination strings). Everything is embedded locally — zero runtime network calls, addressing the user's explicit preference for desktop-local operations over pulling reference data from Supabase. Per follow-up request, HQ province/city was moved out of the Rate Matrix screen into a new read-only "Business" section on Settings, with a "Change" link to re-open the editable form (deliberately not hard-locked, so tier logic can still be exercised during this build phase — logged as ROD008).
Bugs found       : RC001 — SearchableSelect's full-screen invisible overlay silently swallowed clicks meant for other buttons (e.g. Settings "Save") whenever a dropdown was left open; surfaced as "can't save the HQ location." RC002 — after migration 0008 dropped `cities`, three columns (hq_city_id, custom_rates.city_id, bookings.destination_city_id) still declared `references cities(id)`; the real tauri-plugin-sql/sqlx runtime enforces foreign keys (the sqlite3-module dry-run tests used earlier do not), so every write to those columns failed with "no such table: main.cities" — surfaced as the save still silently failing even after RC001 was fixed.
Fixes applied    : FX001 (SearchableSelect outside-click listener, no page-covering overlay) and FX002 (migration 0009 — recreated business_profile/custom_rates/bookings with the three columns repointed at `municipalities(id)`; verified by replaying all 9 migrations with `PRAGMA foreign_keys=ON` and reproducing the exact failing save).
Lessons recorded : Dry-run migration testing via Python's bare `sqlite3` module defaults to `PRAGMA foreign_keys=OFF`, which does NOT match the real app runtime (tauri-plugin-sql/sqlx enforces FKs) — future migration verification should explicitly set `PRAGMA foreign_keys=ON` to catch this class of bug before shipping, not just check for orphaned rows/duplicate ids.
Carried forward  : ROT007 (Supabase Auth wiring, replacing the DEV_BUSINESS_ID placeholder) is the next unstarted item — nothing else queued in NEXT.

## SES007 — 2026-07-26 — Rate Matrix panel, Owners/Registry, and full booking-lifecycle overhaul shipped
Progress made    : ROT008 — replaced the "Expected payment" text on the booking form with a Rate Matrix panel (Tier 1/2/3 rates for the vehicle's seating band, active tier highlighted); pricing tried half-day-rounded billing, then per explicit user call reverted to a single value: exact elapsed hours x (daily rate / 24), rounded up to nearest 50 — half-day/night counts stayed as display-only reference, never re-adopted for billing (ROD009). Added an exact "Xh Ym" duration readout alongside the existing half-day reference without touching the half-day math itself.
ROT009 — built Owners: `owners` table (full name + structured address [province/municipality/free line] required, contact optional/editable later), vehicles now require an owner (ROD011). Added a Registry tab (Vehicle & Owner unified form, plus an Owners subtab) superseding the old standalone Owners screen (couldn't delete the file per the workspace's no-delete rule — overwrote with a deprecation stub instead). `action_logs` table + a diff-based logger (lib/repo/actionLog.ts) records every edit to a field that started optional (chassis/engine number, GPS device/provider/notes, owner contact) — surfaced read-only in Settings > Action History. Fleet vehicle registration simplified: daily rate field removed (Rate Matrix owns pricing now), Owner + seating capacity made the important required fields. Rentals now defaults to the Ongoing subtab instead of History. Investigated the "N pending sync" badge per user worry it was a "ticking time bomb" — confirmed it's an inert local outbox queue with no sync worker yet, so it can't crash the app or hold stale data across a reset.
ROT010 — full booking-lifecycle rework. Added a Save-time confirmation (ArrivalDialog) when a booking's due-back is already in the past, letting staff resolve arrival (not yet / on time / custom time) before the row is even written. Generalized that into the real completion mechanism the app never had (Contract/Inspection/Handover in Checkout are still inert stubs): booking status (pending/active/completed) is now derived from timing + actual_return_at, never chosen directly (ROD010); vehicle rented/available status auto-syncs with it end to end (createBooking, cancelBooking, markBookingReturned, resetAllBookings). Added the symmetric departure-side guard per user request — actual_departure_at, a "Mark departed" action, and a live "departure due" flag for a still-pending booking whose scheduled ETD has elapsed. Overdue return upgraded from a static "due {time}" line to a live real-time "Xh Ym overdue" counter (Home + Rentals). Removed the "awaiting availability check" badge entirely — nothing ever clears it (no sync engine yet), so it was permanently stuck true on every booking regardless of status. Added a live-ticking clock to the Home "Today" header. Added Settings > Dashboard: four independent cosmetic-only toggles (Vehicle→Unit, Customer→Lessee, Start→ETD, End→ETA), Home-screen-only, stored as real per-device settings rather than hardcoded.
Items moved      : ROT008, ROT009, ROT010 → DONE. ROD009, ROD010, ROD011 → DECISIONS (LOCKED). ROT007 unchanged, still NEXT.
Bugs found       : RC003 — none of the new Registry/Vehicle-edit save handlers had try/catch; any thrown DB error (schema mismatch, etc.) rejected silently with zero UI feedback, surfaced by the user as "I can't register the owner and vehicle, nothing happens when I click Register." RC004 — a migration file (0010_owners.sql) was edited after already being applied to the user's local db to add a CHECK-constraint value; tauri-plugin-sql's per-migration checksum then no longer matched, so on relaunch it rejected all migrations and the whole db connection init hung — symptom was the app stuck forever on "Starting up…", console showing "migration 10 was previously applied but has been modified," and every DB-backed action (including RC003's Register button) appearing to do nothing since nothing could ever finish connecting.
Fixes applied    : FX003 (RC003) — added submitError/saveError state + try/catch/finally + red dismissible error banners to VehicleOwnerForm.handleSubmit, OwnerEditRow.handleSave, and VehiclesScreen's VehicleEditRow.handleSave. FX004 (RC004) — deleted the local racos.db file so migrations reapply cleanly against a fresh file; a checksum mismatch on an existing db is unrecoverable short of that.
Lessons recorded : Editing a migration file that may already be applied to someone's local db breaks tauri-plugin-sql's checksum tracking and hangs the entire app on next launch — once a migration file has shipped, treat it as immutable and append a new migration instead of editing history, even for a small CHECK-constraint tweak. Also: Tauri/Vite frontend changes hot-reload, but this class of bug lives in the Rust-embedded migration SQL, so a stuck "Starting up…" screen after any migration-file edit means a full rebuild/restart (or, if the db predates the edit, deleting racos.db) — not a frontend refresh.
Carried forward  : ROT007 (Supabase Auth wiring) remains the only unstarted NEXT item.

## SES008 — 2026-07-27 — Booking safety guards, Settlements/Remittances module, and Tools/Car Activity shipped
Progress made    : ROT011 — Settlements module: Records subtab (payment history) and Remittances subtab (per-vehicle/owner payout report), built on a new resolved_rate booking column plus shared resolveBookingRate/computeExpectedPayment helpers. Overtime payment fields (additional_payment) added to ArrivalDialog and wired through markBookingReturned end to end. The Remittances breakdown (per-rent/12hr/24hr toggle) went through several iterations before landing correctly: the user supplied a reference PDF (3 worked scenarios) and confirmed the app should match it exactly; verified via a node.js simulation against all 9 reference rows that the right model is one shared cash bucket per booking rather than separate scheduled/overtime buckets — a block bills the clean rate-based amount only when it's genuinely full-length and the bucket still covers it, otherwise it absorbs whatever's left and every later block gets 0 (ROD012). Added a compact `R[HH:MM||paid/expected],O[...]` per-booking summary line (Settings toggle, off by default on screen, always shown when printing). Added printing to Remittances: dark-theme-safe print palette, per-row (not whole-table) page-break control to avoid blank-page gaps, a trimmed print header (business name/date+time) with a "Powered by RACOS" footer, and an editable Business name field on Settings > Business.
ROT012 — Booking safety guards: a Save-time total-duration confirmation summary (days + HH:MM) before a backdated/past-rent entry posts, catching accidental absurd durations before they reach history. Actual-return time on an already-recorded booking is now editable (only that field — everything else stays read-only) with an "edited {date}" indicator and edit-history detail. Fleet's Status column made read-only, since it's already fully automatic from booking lifecycle.
ROT013 — Tools tab (new top-level nav item) with its first tool, Car Activity: a per-vehicle month timeline (x-axis = fixed 24h clock, y-axis = days of month), light-blue bars for ETD→ETA, orange for ETA→actual return (including a live-growing bar for a still-overdue booking), vehicle dropdown sourced from Fleet, month navigation, and cursor-following hover tooltips. Added overlap/conflict detection (red bars where two different bookings' ranges genuinely collide on the same day). Two follow-up passes: the day-row grid now measures real available viewport space and self-corrects for row-border/header overhead so a full 28-31 day month renders with no scrollbar; the hover tooltip now measures its own size and flips/clamps to stay fully on-screen instead of being cut off near the right/bottom edge.
Items moved      : ROT011, ROT012, ROT013 → DONE. ROD012 → DECISIONS (LOCKED). ROT007 unchanged, still NEXT.
Bugs found       : RC005 — Remittances breakdown blocks initially split payment into two independent buckets (scheduled hours vs. overtime hours), which reconciled correctly for simple cases but gave the wrong amount for any block straddling the overtime boundary (reference PDF scenario 3: computed 2500 for a 24h block that should bill a clean full-rate 2400). Root cause: no single shared cash bucket, so a block could bill against "overtime money" an earlier block had already spent.
Fixes applied    : FX005 (RC005) — rewrote the breakdown-block loop around one shared `remaining` bucket per booking (base + additional payment combined); a block gets the full rate-based expected amount only if it's genuinely full-length and the bucket can still cover it, otherwise it takes whatever's left and every later block in that booking gets 0. Verified against all 9 rows across the user's 3 reference scenarios via a standalone node.js simulation before touching the real component.
Lessons recorded : The user's own reference spreadsheet had at least one internal inconsistency (an expected-payment figure copied from a different scenario's block); flagged rather than silently matched, and the user confirmed it was an unintentional error in their sheet, not a spec requirement. Reinforced the standing rule from SES007/FX004 — never edit a migration file after it may have shipped, append a new one instead — which held for every schema change this session (resolved_rate column, show_remittance_summary setting, action_log entity_type widening).
Carried forward  : ROT007 (Supabase Auth wiring) remains the only unstarted NEXT item.

## SES009 — 2026-07-28 — Tools > Logs audit trail, cancellation reasons, auto-mark-departed shipped
Progress made    : ROT014 — added a Logs sub-tab to Tools: a flat, most-recent-first table of every booking (not scoped to a vehicle/month like Car Activity), joining listBookings() with listActionLogsByType("booking"). Closed the gap where cancelBooking()/markBookingReturned()/markBookingDeparted() never called logAction() (only updateBookingTimes did) — migration 0019 widened action_logs' CHECK constraint to cover completed/cancelled/departed. Per user follow-up, cancelling a booking now opens a CancelBookingDialog requiring a reason (client never arrived/declined, client returned the unit, or other + free text), permanently logged with the cancellation. A cancellation also snapshots whether the vehicle had already departed at that moment (read from the pre-cancellation row, immune to later Edit-times corrections) — flagged red with a departure-relative variance ("V-HH:MM after/before departure") when it happened after departure, blue/unremarkable otherwise; the reason is shown alongside. Added Settings > Rental > "Auto-mark departed" (on by default): a background AutoDepartureRunner polls every minute and auto-confirms a still-pending booking's departure once ETD passes, tagged "(automatic)" in its log entry so the audit trail can always tell a staff click apart from the system closing the loop on its own (migration 0020 added the setting column). The Logs table also shows a recorded-vs-ETD variance (blue booked-ahead, red backdated) and colors each Ref blue/red for ongoing/cancelled. Iterated on the Logs layout per user feedback: an initial pass replaced the general Actions column with a dedicated Cancelled column, which the user correctly called out as wrong (cancelling is just one action among several — edits, mark-returned, mark-departed — not something deserving its own column); reverted to one unified Actions column listing every event, with cancellation's extra variance/reason detail nested under its own line instead. Later added owner/vehicle filters, five sort modes (recorded/ETD, both directions, plus Ref A-Z), and a print button with print-only header/footer (same pattern as RemittancesReport) — kept scoped entirely to LogsScreen.tsx per the user's explicit "as surgical as possible" instruction.
Items moved      : ROT014 → DONE. ROD013 → DECISIONS (LOCKED). ROT007 unchanged, still NEXT.
Bugs found       : RC006 — migrations 0019 and 0020 were written but never registered in src-tauri/src/lib.rs's hand-maintained migration list (this project doesn't auto-discover .sql files from the folder), so neither ever actually ran. Surfaced when the user reported being unable to toggle several Settings checkboxes at all — updateSettings() now referenced the not-yet-existing auto_mark_departed column in its single shared UPDATE statement, so every settings save failed outright, not just the new one. Retroactively, this also means earlier "cancelled"/"completed"/"departed" log entries had likely been silently failing too, since 0019's widened CHECK constraint never applied.
Fixes applied    : FX006 (RC006) — added the two missing Migration{} entries (version 19, 20) to lib.rs. Confirmed no cargo/rustc available in the sandbox to compile-check the Rust change, so it was verified by manual review against the existing entries' exact pattern instead; user needs a full app restart (not just a frontend reload) for the migrations to actually apply.
Lessons recorded : This project's migrations are registered by hand in lib.rs, not auto-discovered from the migrations folder — dropping a new .sql file in without an accompanying Migration{} entry fails silently (no error, it just never runs). Any future migration work needs to check lib.rs alongside the .sql file, not just the .sql file alone. Also reinforced: when the user pushes back on a design choice ("why did you replace X with Y, aren't they related?"), that's a cue to re-examine the merge rather than defend the original split — the fix here was a straightforward revert-and-merge, not a compromise.
Carried forward  : ROT007 (Supabase Auth wiring) remains the only unstarted NEXT item.

## SES010 — 2026-07-30 — GPS tracking pipeline: Stage 0 and Stage 1 both proven end to end
Progress made    : ROT015 — replacing DAGPS (limited white-label GPS app) with a self-hosted Traccar pipeline for fleet location/history. Architecture: GT06 tracker → Traccar (self-hosted) → Traccar position forwarding (forward.enable/forward.url/forward.type=json, shared secret in the query string since forwarding can't reliably send custom headers) → Supabase Edge Function `gps-ingest` → `vehicle_locations` table → RACOS desktop + Owners' Portal read independently from there; Traccar and RACOS never talk directly. Migration applied to the RACOS Supabase project (ref nnsjqnxvpkercbbwvqjj): added `gps_device_id`/`gps_provider`/`gps_notes` to `vehicles` (unique partial index on gps_device_id) and created `vehicle_locations` (business_id + vehicle_id FKs, normalized lat/lng/speed/course/fix_time columns plus a raw jsonb catch-all for fields not yet normalized, RLS select-only via `business_id = current_business_id()` — no insert/update/delete policy for authenticated users at all; only the Edge Function, via the service/secret key, can write). Deliberately matched the Supabase column names to the local SQLite `vehicles.gps_device_id` field that Registry already exposes (from ROT009/SES007), instead of a separately-named `traccar_device_id` — Falcon's call, so the existing Registry form field becomes the single source of truth once local↔Supabase sync eventually exists, with no translation layer. `gps-ingest` deployed (verify_jwt off, shared-secret auth). Stage 0: proved the Edge Function + database leg alone with a fake curl payload (device 999), cleaned up after. Stage 1: proved the full chain for real — Traccar running in Docker on Falcon's own PC (stand-in for the eventual VPS) and the official Traccar Client Android app (stand-in for the physical GT06) — real GPS positions from a live walk landed correctly in `vehicle_locations`, matched to a test vehicle, then cleaned up.
Items moved      : ROT015 → DONE.
Bugs found       : RC007 — `gps-ingest`'s device-matching logic checked `position.deviceId` (Traccar's internal numeric device ID) before `device.uniqueId` (the actual string identifier registered in Traccar, e.g. "STAGE1TEST") in a `??` fallback chain; since the numeric field is never null, it always won, so every real Traccar-forwarded position 404'd as "no vehicle found" even though the vehicle was registered correctly. Only surfaced once Stage 1 sent a real Traccar-shaped payload — Stage 0's fake curl payload didn't have this field ambiguity, so it couldn't have caught it.
Fixes applied    : FX007 (RC007) — reordered the fallback chain to check `device.uniqueId` first. Diagnosed by adding a temporary `gps_debug_log` table the function wrote raw payloads to (Supabase's `get_logs` MCP tool only returns HTTP boundary request lines, not the function's own `console.log` output, so that route didn't work for inspecting the actual payload shape) — dropped once the fix was confirmed.
Lessons recorded : (1) Traccar's `forward.json` payload carries two different device identifiers — `device.uniqueId` (the real string identifier) and `position.deviceId` (an internal numeric DB id) — and only the former is safe to match against; this wasn't documented anywhere obvious and only showed up by capturing a real payload. (2) Supabase's edge-function `get_logs` doesn't surface function stdout — use a temporary table for payload debugging instead. (3) The sandboxed bash environment's outbound network is allowlist-blocked for `*.supabase.co`, so curl tests against Edge Functions have to run from the user's own machine, not the agent's sandbox. (4) This Traccar server version has self-registration disabled by default (no `admin`/`admin` default account) — needs `users.registration=true` in traccar.xml to create the first account, ideally set back to `false` afterward. (5) Docker Desktop on Windows requires WSL2, which isn't preinstalled — `wsl --install` (elevated) + a full reboot is a real prerequisite step, not optional. (6) Confirmed a genuine, pre-existing architecture gap while scoping this: the RACOS desktop app has zero Supabase connectivity of any kind today (no client library, no Auth, ROT007 still unstarted) and the Owners' Portal doesn't exist as a codebase — so the GPS pipeline is fully proven end to end, but nothing in RACOS itself can display it yet. Also: Supabase's schema has been frozen at its original 5 migrations since SES002 while local SQLite has grown to 20 — worth reconciling before any real local↔Supabase sync work begins.
Carried forward  : ROT007 (Supabase Auth wiring) remains the only unstarted NEXT item in RACOS.md. GPS follow-on work (RACOS desktop read-side / Track Fleet subtab, Owners' Portal build, VPS hosting decision + real GT06 SMS redirect, outbound local→Supabase sync worker, tamper-resistant tracker hardware) logged to PLANS.md as deferred, not yet in active scope.

## SES011 — 2026-08-01 — Map tab, Fleet/Registry restructure, Remittances split modes, payment corrections, Locations toggle shipped
Progress made    : ROT016 — added a real top-level Map tab (vanilla Leaflet, OSM tiles, PH-centered, no vehicle pins yet since there's no live GPS data — a genuine preparatory placeholder, not a UI stub) with a single-select vehicle dropdown defaulting to "All Vehicles". Fleet remodeled to a simplified read-mostly view (Plate/Make-Model/read-only Status/Current Location + "View on Map" link); full vehicle admin moved to a new Registry > Vehicles subtab (migrated wholesale from the old Fleet edit form). Added a car-detail popup on Fleet (click a row, fades on outside click): image, spec fields (Make/Model/Year/Seating/Fuel/Fuel Capacity/Engine/Transmission/GPS/Owner), and an Activity History table (last 10 non-cancelled bookings). Image enlarged to a quarter-page 288x288 frame; added a Fill/Fit toggle set at upload time in Registry with a live preview at the exact popup frame size (migrations 0021 fuel/fuel_capacity/transmission/car_image, 0022 car_image_fit).
ROT017 — Remittances gained a "Recorded split" mode alongside the existing Bucket mode: instead of one block absorbing a shortfall/overtime lump sum, each block's dollar amount is a proportional slice of the actual recorded base+overtime payments by hours (verified against the user's worked 16h/8h, 1400/700 example via a standalone node.js sim, exact match for both 12hr and 24hr breakdowns). Added a Remittance period (From/To, default all-time) that excludes boundary-straddling bookings from totals, with a persistent banner explaining exactly which bookings were excluded and why (verified via a node.js classification sim against a 4-booking scenario). Calendar date pickers in the period filter now highlight (light yellow) any date with booking activity. Fixed a toolbar overflow bug where the Breakdown/Split toggles pushed the Print button off-screen.
ROT018 — Settlements > Records payment correction: Expected payment stays fixed as basis; only the actual recorded Payment (base and/or overtime) can be corrected, additive-only (can never decrease a previously recorded value), capped at that portion's expected rate-formula amount. Every correction now logs to the booking's action-log history per a mid-turn follow-up request (ROD014).
ROT019 — Settings > Locations: new section with Luzon/Visayas/Mindanao visibility toggles (at least one always required on), filtering provinces everywhere they're picked (Rate Matrix, booking destination, owner address, HQ province) — implemented surgically, filtering only at UI option-construction sites (never the raw listProvinces()/listMunicipalities() result, which is also used to resolve historical IDs to labels) and always force-including the currently-selected value so SearchableSelect never blanks out an existing selection (ROD015). Booking form gained up to 10 ranked top-destination quick-pick chips (by most-frequently-picked destination municipality) above the existing Province/City search fields.
Items moved      : ROT016, ROT017, ROT018, ROT019 → DONE. ROD014, ROD015 → DECISIONS (LOCKED). ROT007 unchanged, still NEXT.
Bugs found       : none
Fixes applied    : n/a
Lessons recorded : User repeatedly required clarifying questions before non-trivial implementation work began (Map/Fleet/Registry restructuring, car-detail popup, Recorded-split mode, Remittance period, payment correction, Locations/top-destinations) and corrected one instance of editing prematurely after a handover prompt — reinforced as the standing workflow for this project. Also reinforced the existing island-group/SearchableSelect filtering lesson from this session's own Locations work (see ROD015) as a durable pattern for any future filtered-picker feature.
Carried forward  : ROT007 (Supabase Auth wiring) remains the only unstarted NEXT item.

## SES012 — 2026-08-10 — Owners' Portal scoped, domain locked, ROT007 (Supabase Auth) wired end to end
Progress made    : Design session for the Owners' Portal (ROP007/ROT020) before any code: clarified the actual scenario (a car owner who entrusted their vehicle to a business, needing narrower access than staff, not just another business_id tenant) — established owner_id doesn't exist anywhere yet, and the desktop app's own Registry/Owners tab (ROT009) is local-only, never mirrored to Supabase. Scoped the portal's dashboard (full activity log incl. client name + payment/revenue, vehicle status, no GPS for now, tools deferred) and the owner login model (business issues a one-time access code → owner claims via email → standard Supabase Auth session from then on, reusing Supabase's built-in email flows, no new tech stack). Confirmed the domain purchase racos.app, with the portal on the owners.racos.app subdomain (ROD016) — established this only matters at deploy time (DNS + Supabase redirect allowlist), not during development, and that a free-tier host (Vercel or equivalent) covers it with no required spend. Clarified and locked ROD017: one business per desktop install, local cache single-tenant, Supabase sync is for backup/continuity if a device is lost, not multi-business switching on one machine. Identified a real gap this surfaced: the outbound-only sync design (ROP009) doesn't cover a second device signing in and pulling existing data down — logged as its own ticket, ROT021 (cold-start inbound sync), alongside ROT020 for the portal itself, both blocked by ROT007 at the time.
ROT007 — wired real Supabase Auth end to end, replacing the DEV_BUSINESS_ID/DEV_PROFILE_ID placeholder. Restored the Supabase project (had auto-paused). Added @supabase/supabase-js + lib/supabaseClient.ts (env-driven, .env.local gitignored, .env.example committed). Built lib/auth.ts: signIn, signUpBusiness (creates auth user then businesses+profiles rows), provisionBusinessForUser (covers the email-confirmation-required path, where signup returns no session), signOut, and bootstrapSession() — resolves the real session on launch, mirrors business/profile into local SQLite (existing FK requirement from ROT003 — vehicles/customers/bookings/payments/profiles all reference the local businesses(id) row), and falls back to a cached session (new session_cache table, migration 0024) if launched offline, per the online-once-then-offline model Falcon specified. currentBusinessId()/currentProfileId() in lib/db.ts kept their exact function signatures (no async) and just switched their internal source from a hardcoded constant to in-memory session state, so none of the ~30 existing repo/*.ts call sites needed touching. New LoginScreen.tsx (sign in / signup / complete-profile). Also built reassignLegacyDevData() — an idempotent one-time reassignment that re-parents any local rows still tied to the old DEV_BUSINESS_ID (vehicles, customers, bookings, payments, owners, action_logs, seating_bands, rate_matrix, custom_rates, plus the singleton business_profile/sync_state tables) onto the real signed-in business, so pre-auth local test data isn't orphaned — runs automatically inside bootstrapSession(), no manual SQL needed.
Items moved      : ROT007 → DONE. ROD016, ROD017 → DECISIONS (LOCKED). ROT020, ROT021 → NEXT (unblocked, ROT007 dependency cleared). RACOS.md DONE table trimmed (ROT008-010 folded into one row) to stay under its line budget.
Bugs found       : RC008 — LoginScreen's internal `mode` state is seeded via `useState(initialMode)`, which React only reads on first mount; when App.tsx changed the `initialMode` prop from "signin" to "complete-profile" after a successful sign-in with no profile row yet, React reused the already-mounted LoginScreen instance instead of remounting it, so the prop change was silently ignored — the screen looked frozen on the sign-in form with the Sign In button doing nothing visible, even though the underlying sign-in had actually succeeded (confirmed via Supabase auth.users.last_sign_in_at).
Fixes applied    : FX008 (RC008) — added `key={auth.status}` to the <LoginScreen> element in App.tsx, forcing React to remount (not just re-render) on each auth-state transition so `initialMode` is re-read fresh. Also hardened runBootstrap() with a try/catch so any future unexpected bootstrapSession() failure falls back visibly to the sign-in screen instead of leaving the UI stuck with no feedback.
Lessons recorded : A prop passed as a React useState() initializer only takes effect on first mount — a component reused across a prop change needs either a `key` to force remount, or a useEffect syncing the prop into state; this bit the exact "click a button, nothing visibly happens" failure mode that's flagged this project's own bug log before (RC003) as needing explicit error/loading feedback, but this time the actual state transition had happened correctly server-side and only the UI failed to reflect it — worth checking auth/backend logs directly (Supabase's auth.users table, in this case) before assuming a backend failure when the symptom is "nothing happened."
Carried forward  : ROT020 (Owners' Portal) and ROT021 (cold-start inbound sync) are next up, both unblocked. The local test-data reassignment (reassignLegacyDevData) has shipped in code but should be spot-checked against Falcon's actual local racos.db the next time the app is run, to confirm pre-auth Fleet/Rate Matrix/booking test data reappeared correctly under the real business.

## SES013 — 2026-08-10 — Post-auth polish, Schema Library doc, and owner login-code system (ROT020 prep) shipped
Progress made    : Confirmed reassignLegacyDevData worked correctly on first real relaunch — 4 pre-auth test vehicles reappeared under the real business. Added Settings > Account: signed-in email + a Sign Out button (ConfirmDialog-gated) that clears both the Supabase session and session_cache, for switching between test accounts without reinstalling — surfaced a useful side effect discussed with Falcon: since logout never wipes local data, each signed-into business's rows just persist under their own business_id, so switching accounts behaves like flipping between ledger books (flagged that the outbox has no business_id column, so pending-sync counts mix across accounts if this is used for real multi-business testing).
Home screen gained a real identity header: business name (large), with HQ address/signed-in email/contact number as a subheader line, each shown only if set. New migration 0025 added business_profile.contact_number (optional, editable any time in Settings > Business, unlike the one-time-feeling HQ province/city step). Diagnosed and fixed a real "Loading…" hang on Home/Settings (RC008 companion issue, logged as RC009 below).
Per explicit request, removed the global search bar and every per-tab title heading (`<h1>{TITLES[tab]}</h1>`) from App.tsx — screens now start directly with their own content. The "N pending sync" badge was relocated from the old top header into the sidebar, directly under Settings, only rendered when count > 0.
Wrote SCHEMA_LIBRARY.md (BRAINS/SCHEMA_LIBRARY.md) — a full columns-per-tab reference doc cross-checked directly against live Supabase schema (via list_tables) and all local migrations, plus a Formulas section (pricing, tiering, booking-status derivation, remittance bucket/recorded-split math, payment-correction rule) so Falcon can verify app behavior against real stored logic. Surfaced two real findings while compiling it: the `payments` table exists on both Local and Cloud but is completely unused (no repo file reads/writes it — Settlements works entirely off bookings.payment_amount/expected_payment/additional_payment instead); and Supabase's `bookings`/`vehicles` tables are frozen at their original ROT002 shape (missing destination/purpose/payment-split/actual-timestamp/resolved_rate columns that local gained since). Registered the file in RACOS.md's FILES table, same update cadence as BRAINS (session close / explicit request) per Falcon's instruction — corrected its logged location mid-session after discovering it had actually written to /RACOS/BRAINS/ rather than the project root as first stated.
ROT020 prep — owner login-code system: discussed and locked the design (ROD018/ROD019 from earlier in this session) that the code is a permanent login credential (not a claim-once step), generation requires live Supabase connectivity and IS what creates that owner's Cloud row (not a separate sync step), and no wipe/silent-auto-assign for existing test owners — an explicit per-owner "Generate login code" action instead. Shipped: local migration 0026 (owners.login_code, nullable); new Supabase `owners` table (id, business_id, full_name, login_code unique, timestamps, RLS by business_id, updated_at trigger — applied directly via migration + saved to supabase/migrations/); generateOwnerLoginCode() in lib/repo/owners.ts (8-char code, ambiguous characters excluded, retry-on-collision loop keyed to the unique constraint, upsert by id so re-generation is safe); Registry > Owners UI showing the code read-only once set, or a "Generate login code" button before that. Verified end to end against two real owners via direct SQL against the live Supabase table (correct business_id, correct code, RLS intact).
Items moved      : ROD018, ROD019 (from earlier this session) now have shipped code behind them. No ROT ticket numbers were burned for the Home/Settings/UI-cleanup work or the owner login-code work at time of logging — see Carried forward.
Bugs found       : RC009 — lib/db.ts's session state (`_businessId`/`_profileId`) is plain module-level `let`, not React state. Vite's Fast Refresh preserves component state across live edits but re-executes changed modules' top-level code, resetting these to null while the UI still showed the authenticated shell (App.tsx's `auth`/`ready` state survived, the underlying session did not). Symptom: Home/Settings hung on "Loading…" forever after a live code edit mid-session, with zero error feedback, because none of the fetch chains had `.catch()`.
Fixes applied    : FX009 (RC009) — added `.catch()`/try-catch + a loadError state to HomeScreen and SettingsScreen's two data-loading effects (BusinessName, BusinessHeadquarters), so any future fetch failure shows a red error message instead of hanging silently — same pattern as RC003's fix, extended to read-time loaders. The underlying dev-only desync itself isn't a code bug to fix — confirmed resolved by a full app restart, and not expected to occur in a real production build (no Fast Refresh there).
Lessons recorded : Module-level mutable state (anything outside React's useState/useReducer) does not reliably survive Vite Fast Refresh, even though component state does — a live-editing session can silently desync the two. Worth remembering for any future module-level session/cache state in this codebase. Also reinforced RC003's lesson a second time (RC009): every on-mount data fetch in this codebase needs a `.catch()`, not just save handlers — confirmed by how easily "Loading…" hid a real, checkable error (Supabase's auth.users.last_sign_in_at proved the actual root cause in under a minute once surfaced).
Carried forward  : ROT020's remaining scope: the custom Edge Function for code-based owner login, and the Next.js portal project itself (neither started). ROT021 (cold-start inbound sync) still unstarted. ROP009 (general outbound sync worker) discussed at length (Sync vs Backup button naming, sequencing vs. ROT020) but deliberately not started — Falcon confirmed owner-code work should come first since it's the harder blocker for the portal existing at all.
