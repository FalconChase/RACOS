# Copy to .env.local and fill in with `npx supabase-mcp` / Supabase dashboard values.
# .env.local is gitignored (matches the *.local pattern) — never commit real keys.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
