// DEPRECATED — superseded by the "Owners" subtab inside RegistryScreen.tsx
// (owner creation now happens through the unified Vehicle & Owner form, and
// owner browsing/editing lives in Registry > Owners). This file is no longer
// imported or routed to anywhere; kept as an empty stub rather than deleted
// because files in the connected workspace folder can't be removed without
// asking the user first (see allow_cowork_file_delete). Safe to delete once
// that's confirmed.
export default function OwnersScreen() {
  return null;
}
